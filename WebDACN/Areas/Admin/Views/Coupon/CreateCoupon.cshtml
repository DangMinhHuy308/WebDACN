@model WebDACN.Models.EF.Coupon

@{
    ViewBag.Title = "Thêm mới mã giảm giá";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
@section naviheader{
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/coupon" class="nav-link">Danh mục giảm giá</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/coupon/add" class="nav-link">Thêm mới</a>
        </li>
    </ul>
}


<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Thêm mới mã giảm giá</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/admin/coupon">Danh sách mã giảm giá</a></li>
                    <li class="breadcrumb-item active">Thêm mới</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>



<section class="content">
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Thông tin thêm mới mã giảm giá</h3>
        </div>
        @using (Html.BeginForm("CreateCoupon", "Coupon", FormMethod.Post, new { }))
        {
            @Html.AntiForgeryToken()
            @Html.ValidationSummary(true)
            <div class="form-group">
                <label for="Code">Mã</label>
                @Html.TextBoxFor(x => x.Code, new { @class = "form-control", @placeholder = "Mã" })
                @Html.ValidationMessageFor(x => x.Code, null, new { @class = "text-danger" })
            </div>
            <div class="form-group">
                <label for="Name">Tên</label>
                @Html.TextBoxFor(x => x.Name, new { @class = "form-control", @placeholder = "Tên" })
                @Html.ValidationMessageFor(x => x.Name, null, new { @class = "text-danger" })
            </div>
            <div class="form-group">
                <label for="Type">Loại</label>
                @Html.DropDownListFor(x => x.Type, new SelectList(new[] { new { Value = "1", Text = "Giảm giá phần trăm" }, new { Value = "2", Text = "Giảm giá số tiền cố định" } }, "Value", "Text"), new { @class = "form-control", id = "Type" })
            </div>
            <div class="form-group">
                <label for="Number">Số tiền giảm</label>
                @Html.TextBoxFor(x => x.Number, new { @class = "form-control", @placeholder = "Số tiền giảm" })
                @Html.ValidationMessageFor(x => x.Number, null, new { @class = "text-danger" })
            </div>
            @*<div class="form-group">
                    <label for="Start_date">Ngày bắt đầu</label>
                    @Html.TextBoxFor(x => x.StartDate, "{0:yyyy-MM-dd}", new { @class = "form-control", @type = "date" })
                </div>
                <div class="form-group">
                    <label for="End_date">Ngày hết hạn</label>
                    @Html.TextBoxFor(x => x.EndDate, "{0:yyyy-MM-dd}", new { @class = "form-control", @type = "date" })
                </div>*@
            <div class="form-group">
                <label for="Is_active">Trạng thái</label>
                @Html.DropDownListFor(x => x.IsActive, new SelectList(new[] { new { Value = "true", Text = "Kích hoạt" }, new { Value = "false", Text = "Không kích hoạt" } }, "Value", "Text"), new { @class = "form-control", id = "Is_active" })
            </div>
            <button type="submit" class="btn btn-primary">Tạo</button>
        }
    </div>
</section>